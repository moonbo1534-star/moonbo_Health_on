{
    "name": "daily-workout-planner",
    "version": "1.0.0",
    "description": "Daily Workout Planner App",
    "main": "index.html",
    "scripts": {
        "build": "echo 'No build step required for static site'",
        "android:init": "npm install && npx cap add android",
        "android:sync": "npx cap sync",
        "android:open": "npx cap open android"
    },
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/core": "^6.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^6.0.0"
    }
}